version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      # Cache npm's tarball store — npm ci uses this to reinstall
      # without re-downloading from the registry (saves ~90s per build).
      # Do NOT cache node_modules itself; npm ci always deletes it.
      - ~/.npm/**/*
